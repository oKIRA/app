"use client";

import { useEffect } from "react";
import "../firebase";

export function FirebaseProvider() {
  useEffect(() => {
    // Firebase é inicializado no lado do cliente
  }, []);

  return null;
}
